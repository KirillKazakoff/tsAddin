import tablesStore from '../../../stores/tablesStore/tablesStore';
import { InnerRowT } from '../../../types/typesTables';

export const initRequestContractSection = () => {
    const { innerT } = tablesStore;

    const getRequest = async (row: InnerRowT) => {};
};
