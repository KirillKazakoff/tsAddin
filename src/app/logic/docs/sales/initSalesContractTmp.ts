import { initSalesContractPartT } from '../../../types/typesInitTmp';

export const initSalesContractTmp: initSalesContractPartT = (book, contract) => {
    const ws = book.getWorksheet('Contract_Live');
};
